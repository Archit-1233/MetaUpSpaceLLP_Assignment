# Core
transformers>=4.41.0
torch>=2.1.0; platform_system != "Darwin"
torch>=2.2.0; platform_system == "Darwin"
accelerate>=0.30.0

# Optional UI
streamlit>=1.34.0

# Optional API (not used by default but handy)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

matplotlib
